@*@model IList<OrderAverageReportLineSummaryModel>*@
@model TodayReportModel
@using Kendo.Mvc;
@using Kendo.Mvc.UI

<div class="widget stats-today">
    <div class="widget-content widget-title">
        <div class="stats-today-title" style="line-height:52px;">
            @T("Admin.Users.Reports.Today")
        </div>
        <div class="stats-today-item omega">
            <div class="stats-today-item-label">@T("Admin.Users.Reports.TodayReport.Fields.NowConnected")</div>
            <div class="stats-today-item-value">@Model.NowConnectedDevices</div>
        </div>
        <div class="stats-today-item">
            <div class="stats-today-item-label">@T("Admin.Users.Reports.TodayReport.Fields.TodayConnected")</div>
            <div class="stats-today-item-value">@Model.TodayConnected</div>
        </div>
        <div class="stats-today-item">
            <div class="stats-today-item-label">@T("Admin.Users.Reports.TodayReport.Fields.Active")</div>
            <div class="stats-today-item-value">@Model.TodayActiveDevices</div>
        </div>
        <div class="stats-today-item">
            <div class="stats-today-item-label">@T("Admin.Users.Reports.TodayReport.Fields.Deactivated")</div>
            <div class="stats-today-item-value">@Model.TodayDeactivatedDevices</div>
        </div>
        <div class="stats-today-item alfa">
            <div class="stats-today-item-label">@T("Admin.Users.Reports.TodayReport.Fields.UsersOnline")</div>
            <div class="stats-today-item-value">@Model.UsersOnline</div>
        </div>
    </div>
</div>
<div class="widget widget-table">
    <div class="widget-header">
        <i class="icon-signal"></i>
        @*<h3>@T("Admin.SalesReport.Average")</h3>*@
    </div>
    <div class="widget-content">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    @*<th>@T("Admin.SalesReport.Average.OrderStatus")</th>
                    <th>@T("Admin.SalesReport.Average.SumThisWeekOrders")</th>
                    <th>@T("Admin.SalesReport.Average.SumThisMonthOrders")</th>
                    <th>@T("Admin.SalesReport.Average.SumThisYearOrders")</th>
                    <th>@T("Admin.SalesReport.Average.SumAllTimeOrders")</th>*@
                </tr>
            </thead>
            <tbody>
                @*@foreach (var x in Model)
                {
                    <tr>
                        <td>@x.OrderStatus</td>
                        <td>@x.SumThisWeekOrders</td>
                        <td>@x.SumThisMonthOrders</td>
                        <td>@x.SumThisYearOrders</td>
                        <td>@x.SumAllTimeOrders</td>
                    </tr>
                }*@
            </tbody>
        </table>
    </div>
</div>
